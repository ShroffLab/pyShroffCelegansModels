# Python Wrapper Code for Shroff Lab C Elegans Untwisting Models

## Setup
`pixi run setup_julia_env` 

To get new versions of julia packages, might need to `pixi clean` before you run that command again.